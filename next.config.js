// For Visual Reference - The complete code for: next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  // We can add configuration options here in the future if needed
};

module.exports = nextConfig;